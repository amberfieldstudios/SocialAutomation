{
  "name": "@social/conformance",
  "version": "0.1.0",
  "private": true,
  "description": "Shared PlatformConnector conformance harness. runConformance(factory, capabilities, mockEnv) exercises any connector plugin against the contract using a mocked HTTP env — no real credentials.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@social/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.5"
  },
  "peerDependencies": {
    "vitest": "^2.0.5"
  }
}
